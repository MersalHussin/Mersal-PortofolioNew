name: Keep Supabase Alive

on:
  schedule:
    # Runs every 3 days at midnight UTC
    - cron: '0 0 */3 * *'
  workflow_dispatch: # Allows manual trigger from the GitHub Actions tab

jobs:
  ping-supabase:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase API
        run: |
          curl -X GET "https://bbqjgjzhetnrvmmtetry.supabase.co/rest/v1/projects?select=id&limit=1" \
          -H "apikey: sb_publishable_9YkiVnY-lvX6hboi8KpMNA_I9-K771_" \
          -H "Authorization: Bearer sb_publishable_9YkiVnY-lvX6hboi8KpMNA_I9-K771_"
